# Fixture mobile

Copie para esta pasta `test_image.jpg`, `expected_embedding.json` e `model_info.json` gerados por `VisualMatch.API/generate_compatibility_reference.py`. O modo de compatibilidade usa `preprocessImage`, `generateEmbedding` e `compareEmbeddings`, o mesmo caminho de inferência do app sem o crop da câmera.
